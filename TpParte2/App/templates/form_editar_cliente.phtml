<?php
$pageTitle = 'Editar Cliente';
require __DIR__ . '/layout/header.phtml';
?>

<div class="contenedor-formulario">
    <h2 class="titulo-principal">Editar Cliente</h2>

    <form action="<?= BASE_URL ?>update-cliente/<?= $cliente->id_cliente ?>" method="POST" class="formulario-alta">

        <div class="grupo-form">
            <label for="nombre">Nombre:</label>
            <input type="text" id="nombre" name="nombre" value="<?= htmlspecialchars($cliente->nombre) ?>" required>
        </div>

        <div class="grupo-form">
            <label for="email">Email:</label>
            <input type="email" id="email" name="email" value="<?= htmlspecialchars($cliente->email) ?>" required>
        </div>

        <div class="grupo-form">
            <label for="imagen_url">Imagen (URL, opcional):</label>
            <input type="url" id="imagen_url" name="imagen_url" value="<?= htmlspecialchars($cliente->imagen_url ?? '') ?>" placeholder="https://...">
        </div>

        <button type="submit" class="boton-agregar">Guardar Cambios</button>
        <a href="<?= BASE_URL ?>clientes" class="enlace-cancelar">Cancelar y volver al listado</a>
    </form>
</div>

<?php require __DIR__ . '/layout/footer.phtml'; ?>
