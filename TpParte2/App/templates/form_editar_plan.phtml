<?php
$pageTitle = 'Editar Categoría';
require __DIR__ . '/layout/header.phtml';
?>

<div class="contenedor-formulario">
    <h2 class="titulo-principal">Editar Categoría</h2>

    <form action="<?= BASE_URL ?>update-categoria/<?= $plan->id_plan ?>" method="POST" class="formulario-alta">

        <div class="grupo-form">
            <label for="nombre">Nombre de la categoría:</label>
            <input type="text" id="nombre" name="nombre" value="<?= htmlspecialchars($plan->nombre) ?>" required>
        </div>

        <div class="grupo-form">
            <label for="descripcion">Descripción:</label>
            <input type="text" id="descripcion" name="descripcion" value="<?= htmlspecialchars($plan->descripcion) ?>" required>
        </div>

        <div class="grupo-form">
            <label for="monto_total">Monto Total ($):</label>
            <input type="number" step="0.01" id="monto_total" name="monto_total" value="<?= htmlspecialchars($plan->monto_total) ?>" required>
        </div>

        <div class="grupo-form">
            <label for="imagen_url">Imagen (URL, opcional):</label>
            <input type="url" id="imagen_url" name="imagen_url" value="<?= htmlspecialchars($plan->imagen_url ?? '') ?>" placeholder="https://...">
        </div>

        <button type="submit" class="boton-agregar">Guardar Cambios</button>
        <a href="<?= BASE_URL ?>categorias" class="enlace-cancelar">Cancelar y volver al listado</a>
    </form>
</div>

<?php require __DIR__ . '/layout/footer.phtml'; ?>
