<?php
$pageTitle = 'Agregar Nueva Cuota';
require __DIR__ . '/layout/header.phtml';
?>

<div class="contenedor-formulario">
    <h2 class="titulo-principal">Alta de Nueva Cuota (Ítem)</h2>

    <form action="<?= BASE_URL ?>add-item" method="POST" class="formulario-alta">

        <div class="grupo-form">
            <label for="id_plan">Categoría (Plan de Cuotas):</label>
            <select id="id_plan" name="id_plan" required>
                <option value="" disabled selected>Seleccione una categoría...</option>
                <?php foreach ($planes as $plan): ?>
                    <option value="<?= $plan->id_plan ?>"><?= htmlspecialchars($plan->nombre) ?></option>
                <?php endforeach; ?>
            </select>
        </div>

        <div class="grupo-form">
            <label for="id_cliente">Cliente:</label>
            <select id="id_cliente" name="id_cliente" required>
                <option value="" disabled selected>Seleccione un cliente...</option>
                <?php foreach ($clientes as $cliente): ?>
                    <option value="<?= $cliente->id_cliente ?>"><?= htmlspecialchars($cliente->nombre) ?></option>
                <?php endforeach; ?>
            </select>
        </div>

        <div class="grupo-form">
            <label for="nro_cuota">Número de cuota:</label>
            <input type="number" id="nro_cuota" name="nro_cuota" min="1" required>
        </div>

        <div class="grupo-form">
            <label for="monto">Monto ($):</label>
            <input type="number" step="0.01" id="monto" name="monto" required>
        </div>

        <div class="grupo-form grupo-checkbox">
            <label for="estado_pago">¿Está pagada?</label>
            <input type="checkbox" id="estado_pago" name="estado_pago">
        </div>

        <div class="grupo-form">
            <label for="imagen_url">Imagen (URL, opcional):</label>
            <input type="url" id="imagen_url" name="imagen_url" placeholder="https://...">
        </div>

        <button type="submit" class="boton-agregar">Guardar Cuota</button>
        <a href="<?= BASE_URL ?>items" class="enlace-cancelar">Cancelar y volver al listado</a>
    </form>
</div>

<?php require __DIR__ . '/layout/footer.phtml'; ?>
